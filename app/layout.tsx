import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sideline AI — Demo",
  description:
    "Front-end demo of Sideline AI: record in-person sales conversations, transcribe and summarize them automatically, and review every call across your team. Sample data only.",
};

export const viewport: Viewport = {
  // Matches the light workspace background (`--background`) so browser chrome
  // blends into the page. Web is light-only; the mobile app stays dark.
  themeColor: "#f5f6f4",
  colorScheme: "light",
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full">{children}</body>
    </html>
  );
}
